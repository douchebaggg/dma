{
  "private": true,
  "workspaces": [
    "frontend",
    "frappe-ui"
  ],
  "scripts": {
    "postinstall": "patch-package",
    "dev": "cd frontend && yarn dev",
    "test": "jest",
    "build": "cd frontend && yarn build"
  },
  "dependencies": {
    "patch-package": "^8.0.0"
  }
}
